
---

# ✅ **What’s remaining in List Interface (to complete it 100%)?**

## ✔ 1. LinkedList

* Doubly Linked List implementation
* Good for insert/remove
* Slower for random access
  *(You already started learning this — just small recap needed)*

---

## ✔ 2. Vector

* Older class
* Synchronized (thread-safe)
* Rarely used today
* But asked in interviews:

  * “ArrayList vs Vector?”
  * “Why Vector is legacy?”

---

## ✔ 3. Stack

* Extends Vector
* Legacy class
* Methods:

  * push()
  * pop()
  * peek()
* Rarely used → replaced by `Deque` (`ArrayDeque`)

---

## ✔ 4. List-specific methods

You should know these:

### **Insert:**

```java
add(int index, E element)
```

### **Replace:**

```java
set(int index, E element)
```

### **Search:**

```java
indexOf()
lastIndexOf()
contains()
```

### **Remove:**

```java
remove(int index)
remove(Object o)
```

### **Sublist:**

```java
subList(fromIndex, toIndex)
```

---

## ✔ 5. ListIterator

More powerful than Iterator:

* Move forward → next()
* Move backward → previous()
* Can add, remove, update during iteration

Example:

```java
ListIterator<String> it = list.listIterator();
while(it.hasNext()) {
    System.out.println(it.next());
}
```

---

# 🎯 **If You Know These → You Fully Completed List Interface**

### ✔ ArrayList (deep)

### ✔ LinkedList

### ✔ Vector

### ✔ Stack

### ✔ List methods

### ✔ ListIterator

You already mastered the **hardest part** → ArrayList.

Now, to “complete” List interface, we just need to cover small remaining topics.

---
